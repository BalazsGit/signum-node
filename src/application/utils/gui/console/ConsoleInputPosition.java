package application.utils.gui.console;

/**
 * Defines the vertical position of the command input panel within a console layout.
 */
public enum ConsoleInputPosition {
    /** Command input placed at the top of the console output area */
    TOP,

    /** Command input placed at the bottom of the console output area (default) */
    BOTTOM
}