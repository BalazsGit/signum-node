#######################################################################
# JUnit chain-test mock node profile                                  #
# (v4 profile schema: {confRoot}/node/profiles/{name}.properties)     #
#######################################################################
#
# Mock network: offline, mock mining (accepts any nonce) and a
# hermetic local SQLite database — see signum.net.MockNetwork, which
# itself pins DB.Url to ./db/signum.sqlite-mock.db and enables
# DEV.offline / DEV.mockMining.
node.network = signum.net.MockNetwork
